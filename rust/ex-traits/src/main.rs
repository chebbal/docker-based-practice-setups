fn main() {
    println!("Exercise- Traits and Generics");
    println!("{}","-".repeat(20));
    // Traits are rust's answer to interfaces, abstract base classes,
    // operator overloading. In C++, These are equivalent to virtual functions, CRTP,
    // and concepts. For C developers, traits are structured way to do polymorphism

    
}
